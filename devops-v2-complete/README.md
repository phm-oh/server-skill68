# 🚀 DevOps Setup สำหรับการแข่งขันทักษะวิชาชีพ

## ระบบประเมินบุคลากรด้วยระบบเทคโนโลยีสารสนเทศสมัยใหม่

---

## 📋 Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    เครื่องนักเรียน (Client)                    │
│                    - เขียน Code                              │
│                    - git push                                │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                 VM1: 192.168.100.101                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   GitLab    │  │   Runner    │  │  Registry   │         │
│  │   :80       │  │             │  │   :5000     │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐                          │
│  │ Registry UI │  │  Portainer  │                          │
│  │   :8080     │  │   :9000     │                          │
│  └─────────────┘  └─────────────┘                          │
└─────────────────────┬───────────────────────────────────────┘
                      │ Deploy
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                 VM2: 192.168.100.102                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  Frontend   │  │   Backend   │  │   MySQL     │         │
│  │   :80       │  │   :3000     │  │   :3306     │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌─────────────┐                                           │
│  │ phpMyAdmin  │  Production Server                        │
│  │   :8081     │  Domain: it.udvc.it                       │
│  └─────────────┘                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 ไฟล์ในชุดนี้

```
devops-v2/
├── 01-setup-vm1-gitlab.sh      # ติดตั้ง VM1 (GitLab Server)
├── 02-register-runner.sh       # Register GitLab Runner
├── 03-setup-vm2-production.sh  # ติดตั้ง VM2 (Production)
├── 04-setup-client.sh          # ตั้งค่าเครื่องนักเรียน
├── README.md                   # คู่มือนี้
└── templates/
    ├── .gitlab-ci.yml          # ตัวอย่าง CI/CD Pipeline
    ├── backend/
    │   └── Dockerfile          # Dockerfile สำหรับ Backend
    ├── frontend/
    │   └── Dockerfile          # Dockerfile สำหรับ Frontend
    └── production/
        └── docker-compose.yml  # docker-compose สำหรับ VM2
```

---

## 🔧 ขั้นตอนการติดตั้ง

### Step 1: ติดตั้ง VM1 (GitLab Server)

```bash
# SSH เข้า VM1
ssh user@192.168.100.101

# สร้างโฟลเดอร์
mkdir -p ~/devops-setup
cd ~/devops-setup

# วิธีที่ 1: Copy-paste script ทั้งหมด
nano 01-setup-vm1-gitlab.sh
# (paste เนื้อหา แล้ว Ctrl+O, Enter, Ctrl+X)

# รัน script
chmod +x 01-setup-vm1-gitlab.sh
./01-setup-vm1-gitlab.sh

# รอ GitLab พร้อม (3-5 นาที)
sudo docker logs -f gitlab

# ดู Root Password
sudo docker exec -it gitlab grep 'Password:' /etc/gitlab/initial_root_password
```

### Step 2: Register GitLab Runner

```bash
# 1. เข้า http://192.168.100.101 (GitLab)
# 2. Login ด้วย root + password
# 3. ไป Admin Area > CI/CD > Runners > New instance runner
# 4. Copy token

# รัน script
./02-register-runner.sh glrt-xxxxxxxxxxxx
```

### Step 3: ติดตั้ง VM2 (Production)

```bash
# SSH เข้า VM2
ssh user@192.168.100.102

# สร้างโฟลเดอร์และ copy script
mkdir -p ~/devops-setup
cd ~/devops-setup
nano 03-setup-vm2-production.sh
# (paste เนื้อหา)

# รัน
chmod +x 03-setup-vm2-production.sh
./03-setup-vm2-production.sh
```

### Step 4: วาง docker-compose.yml บน VM2

```bash
# ยังอยู่ใน VM2
sudo mkdir -p /opt/app
cd /opt/app

# สร้าง docker-compose.yml
sudo nano docker-compose.yml
# (paste เนื้อหาจาก templates/production/docker-compose.yml)
```

### Step 5: ตั้งค่าเครื่อง Client (นักเรียน)

**Windows:**
1. แก้ไข `C:\Windows\System32\drivers\etc\hosts`
2. เพิ่ม:
```
192.168.100.101 gitlab.local registry.local
192.168.100.102 prod.local
```

**Linux/Mac:**
```bash
echo "192.168.100.101 gitlab.local registry.local" | sudo tee -a /etc/hosts
echo "192.168.100.102 prod.local" | sudo tee -a /etc/hosts
```

---

## 🎯 Flow การใช้งานสำหรับนักเรียน

### 1. สร้าง Project ใน GitLab
- เข้า http://192.168.100.101
- สร้าง account หรือ login ด้วย root
- สร้าง Project ใหม่

### 2. Clone และเขียน Code
```bash
# Clone repo
git clone http://192.168.100.101/username/project.git
cd project

# สร้างโครงสร้าง
mkdir backend frontend

# เขียน code...
```

### 3. เพิ่มไฟล์ที่จำเป็น
- `backend/Dockerfile`
- `frontend/Dockerfile`
- `.gitlab-ci.yml` (ที่ root)

### 4. Push Code
```bash
git add .
git commit -m "Add application code"
git push origin main
```

### 5. ดู Pipeline
- เข้า GitLab > Project > CI/CD > Pipelines
- รอ Pipeline รันเสร็จ
- ถ้าสำเร็จ image จะอยู่ใน Registry

### 6. Deploy บน VM2
```bash
# SSH เข้า VM2
ssh user@192.168.100.102
cd /opt/app

# Pull และ Run
docker compose pull
docker compose up -d
```

### 7. ทดสอบ
- Frontend: http://192.168.100.102
- Backend API: http://192.168.100.102:3000
- phpMyAdmin: http://192.168.100.102:8081

---

## 🌐 URLs สรุป

| Service | URL |
|---------|-----|
| **GitLab** | http://192.168.100.101 |
| **Registry API** | http://192.168.100.101:5000 |
| **Registry UI** | http://192.168.100.101:8080 |
| **Portainer** | http://192.168.100.101:9000 |
| **Production Frontend** | http://192.168.100.102 |
| **Production Backend** | http://192.168.100.102:3000 |
| **phpMyAdmin** | http://192.168.100.102:8081 |

---

## 🐛 Troubleshooting

### GitLab ไม่ขึ้น
```bash
docker logs gitlab
docker exec -it gitlab gitlab-ctl status
```

### Runner ไม่ทำงาน
```bash
docker logs gitlab-runner
docker exec -it gitlab-runner gitlab-runner verify
```

### Push image ไม่ได้
```bash
# ตรวจสอบ insecure registry
cat /etc/docker/daemon.json
# ต้องมี: "insecure-registries": ["192.168.100.101:5000"]

# Restart docker
sudo systemctl restart docker
```

### Container ไม่เห็นกัน
```bash
# ตรวจสอบ network
docker network ls
docker network inspect app_net
```

---

## ✅ Checklist

### VM1 (GitLab Server)
- [ ] Docker ติดตั้งแล้ว
- [ ] GitLab รันได้ (http://192.168.100.101)
- [ ] Registry รันได้ (http://192.168.100.101:5000)
- [ ] Runner registered และ online

### VM2 (Production)
- [ ] Docker ติดตั้งแล้ว
- [ ] insecure-registry ตั้งค่าแล้ว
- [ ] docker-compose.yml พร้อม

### Client (เครื่องนักเรียน)
- [ ] hosts file แก้แล้ว
- [ ] git clone ได้
- [ ] git push ได้

### CI/CD
- [ ] Pipeline รันได้
- [ ] Build image สำเร็จ
- [ ] Push to registry สำเร็จ
- [ ] Deploy ได้

---

**Version:** 2.0  
**Date:** 2024  
**For:** การแข่งขันทักษะวิชาชีพ ปวช.
